This directory contains the source of SVGMapTools.
It also contains a jar file make script (MakeClass.bat) for Windows and a simple test script (TestTools.bat).
However, since the code of SVGMapTools are created only with Java, it can be used with linux etc.